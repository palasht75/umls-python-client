# Changelog

## 1.1.0

- Rebuilt the project as an installable `src/` layout Python package.
- Added typed sync and async clients, shared `httpx` transport, structured errors,
  and compatibility import shims.
- Added CI, packaging metadata, mocked tests, and build verification.
